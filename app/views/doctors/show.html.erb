<h2><%= @doctor.name %>'s Appointments</h2>

<% @doctor.appointments.each do |appt| %>
<ul>
  <li>
    <%= appt.appt_time %> // <small><%= link_to appt.patient.name, patient_path(appt.patient) %></small>
  </li>
</ul>
<% end %>
