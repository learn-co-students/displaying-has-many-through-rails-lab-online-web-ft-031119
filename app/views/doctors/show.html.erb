<ul>
<% @doctor.appointments.each do |appointment| %>
	<li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M ")
%></li>
	<li><%= link_to appointment.patient.name, patient_path(appointment.patient.id) %></li><br>
<%end%>
</ul> 