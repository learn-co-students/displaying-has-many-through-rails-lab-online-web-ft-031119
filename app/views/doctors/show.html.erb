<h1><%=@doctor.name%></h1>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <% formatted_time =  appt.appointment_datetime.strftime('%B %d, %Y at %H:%M')%>
    <li>
      <%= link_to appt.patient.name, patient_path(appt.patient) %>
      <%= formatted_time %>
    </li>
  <% end %>
</ul>
