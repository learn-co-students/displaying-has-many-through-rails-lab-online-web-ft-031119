<h1>Patient#show</h1>
<% @patient.name %><br>
<% @patient.appointments.each do |app| %>
	<%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
	Doctor Name: <%= link_to app.doctor.name, doctor_path(app.doctor) %><br><br>
<% end %>